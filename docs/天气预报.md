# 1. HTTP天气预报

## 1.1 效果展示

![image-20260627182952953](./image-20260627182952953.png)

# 2. JSON数据格式

![image-20260627201943675](./image-20260627201943675.png)

## 2.1 什么是JSON

![image-20260627183125525](./image-20260627183125525.png)

![image-20260627183434077](./image-20260627183434077.png)

## 2.2 JSON的两种数据格式

### 2.2.1 JSON数组

![image-20260627183520435](./image-20260627183520435.png)

![image-20260627183619035](./image-20260627183619035.png)

### 2.2.2 JSON对象

![image-20260627183642357](./image-20260627183642357.png)

## 2.3 JSON在线解析

https://www.json.cn/

https://www.sojson.com/

![image-20260627183701673](./image-20260627183701673.png)

![image-20260627184001379](./image-20260627184001379.png)

```json
{
	"title": "json在线解析（简版） - soJSON在线解析",
	"domain": "https://www.sojson.com/simple_json_beta.html",
	"keywords": "json在线解析",
	"功能": ["JSON美化", "JSON数据类型显示", "JSON数组显示角标", "高亮显示", "错误提示", {
		"备注": ["www.sojson.com", "json.sojson.com"]
	}],
	"加入我们": {
		"qq群": "259217951"
	}
}
```

## 2.4 Qt 中使用JSON

![image-20260627184715259](./image-20260627184716400.png)

### 2.4.1 JSON相关的类

#### (1)QJsonObject-JSON对象

![image-20260627184922961](./image-20260627184922961.png)

![image-20260627185041490](./image-20260627185041490.png)

#### (2)QJsonArray-JSON数组

![image-20260627185050061](./image-20260627185050061.png)

![image-20260627185135351](./image-20260627185135351.png)

#### (3)QJsonValue-JSON数据类型

![image-20260627185159367](./image-20260627185159367.png)

![image-20260627185211093](./image-20260627185211093.png)

![image-20260627185319994](./image-20260627185319994.png)

![image-20260627185333327](./image-20260627185333327.png)

#### (4)QJsonDocument-JSON文档

![image-20260627185407088](./image-20260627185407088.png)

![image-20260627185601896](./image-20260627185601896.png)

### 2.4.2 构建JSON字符串

![image-20260627185837714](./image-20260627185837714.png)

- 代码

```c++
#include <QCoreApplication>
#include <QJsonObject>
#include <QJsonArray>
#include <QJsonDocument>

//文件的写入
#include <QFile>
//打印输出
#include <QDebug>

//构建JSON
void writeJosn()
{
    QJsonObject rootObj;
    //1. 插入name字段
    rootObj.insert("name","China");

    //2. 插入info字段
    QJsonObject infoObj;
    infoObj.insert("capital","beijing");
    infoObj.insert("asian",true);
    infoObj.insert("founded",1949);
    rootObj.insert("info",infoObj);

    //3. 插入provinces字段
    QJsonArray provincesArray;
    QJsonObject provincesdObj;
    provincesdObj.insert("name","shandong");
    provincesdObj.insert("capital","jinan");

    QJsonObject provincezjObj;
    provincezjObj.insert("name","zhejiang");
    provincezjObj.insert("capital","hangzhou");

    provincesArray.append(provincesdObj);
    provincesArray.append(provincezjObj);

    rootObj.insert("provinces",provincesArray);

    //4.转换为json字符串
    // 以QJsonObject为参数来创建QJsonDocument对象
    // 将QJsonDocument对象转为:JSON字符串
    QJsonDocument doc(rootObj);
    QByteArray json = doc.toJson();

    //5.1 打印输出
    qDebug()<<QString(json).toUtf8().data();

    //5.2 将json字符串写入文件
    QFile file("d:\\china.json");
    file.open(QFile::WriteOnly);
    file.write(json);
    file.close();
    qDebug()<<"已写入到文件中";
}

int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);

    writeJosn();

    return a.exec();
}

```

```c++
{
    "info": {
        "asian": true,
        "capital": "beijing",
        "founded": 1949
    },
    "name": "China",
    "provinces": [
        {
            "capital": "jinan",
            "name": "shandong"
        },
        {
            "capital": "hangzhou",
            "name": "zhejiang"
        }
    ]
}

已写入到文件中
```

### 2.4.3 解析JSON字符串

![image-20260627191641920](./image-20260627191641920.png)

- 代码

```
 
```



# 3. HTTP通信

![image-20260627201913088](./image-20260627201913088.png)

## 3.1 HTTP概述

![image-20260627202319902](./image-20260627202319902.png)

### 3.1.1 两种架构

![image-20260627202410110](./image-20260627202410110.png)

### 3.1.2 基于请求响应的模式

![image-20260627202510548](./image-20260627202510548.png)

### 3.1.3 无状态

![image-20260627202552247](./image-20260627202552247.png)

### 3.1.4 请求报文

![image-20260627202724916](./image-20260627202724916.png)

![image-20260627202755814](./image-20260627202755814.png)

![image-20260627202806211](./image-20260627202806211.png)

### 3.1.5 响应报文

![image-20260627203002783](./image-20260627203002783.png)

![image-20260627203018896](./image-20260627203018896.png)

### 3.1.6 请求方式

![image-20260627214352602](./image-20260627214352602.png)

## 3.2 Postman

![image-20260627214719682](./image-20260627214719682.png)



![image-20260627215636781](./image-20260627215636781.png)

![image-20260627215730406](./image-20260627215730406.png)



![image-20260627221504092](./image-20260627221504092.png)

```url
http://t.weather.itboy.net/api/weather/city/101010100
```

## 3.3 Qt实现HTTP

### 3.3.1创建“网络访问管理”对象

### 3.3.2关联信号槽

![image-20260627221800250](./image-20260627221800250.png)

### 3.3.3发送请求

![image-20260627221859044](./image-20260627221859044.png)

### 3.3.4接收数据

![image-20260627222412582](./image-20260627222412582.png)

# 4. 详细实现步骤

![image-20260627222520020](./image-20260627222520020.png)

# 4.1 新建工程，右键退出

![image-20260627222947266](./image-20260627222947266.png)

## （1）新建工程

## （2）添加资源文件

## （3）设置窗口固定大小，无边框

![image-20260627230415631](./image-20260627230415631.png)

## （4）右键退出

![image-20260627230441262](./image-20260627230441262.png)

### widget.h

```c++
#ifndef WIDGET_H
#define WIDGET_H

#include <QWidget>
#include <QContextMenuEvent>

QT_BEGIN_NAMESPACE
namespace Ui {
class Widget;
}
QT_END_NAMESPACE

class Widget : public QWidget
{
    Q_OBJECT

public:
    Widget(QWidget *parent = nullptr);
    ~Widget();
protected:
    //捕获用户在窗口上点击鼠标右键（或触控板双指点击）的事件，以便弹出自定义的菜单（右键菜单/上下文菜单）。
    void contextMenuEvent(QContextMenuEvent *event) override;
private:
    Ui::Widget *ui;
    QMenu *mExitMenu;   //右键退出的菜单
    QAction *mExitAct;  //右键退出的菜单项(退出的行为)
};
#endif // WIDGET_H

```

### widget.cpp

```c++
#include "widget.h"
#include "ui_widget.h"
#include <QMenu>
#include <QAction>

Widget::Widget(QWidget *parent)
    : QWidget(parent)
    , ui(new Ui::Widget)
{
    ui->setupUi(this);
    setWindowFlag(Qt::FramelessWindowHint);//设置窗口无边框
    setFixedSize(width(),height());         //设置固定窗口大小
    //构建右键菜单,退出程序
    mExitMenu = new QMenu(this);
    mExitAct = new QAction();

    mExitAct->setText("退出");
    mExitAct->setIcon(QIcon(":/res/close.png"));

    mExitMenu->addAction(mExitAct);

    connect(mExitAct,&QAction::triggered,this,[=](){
        qApp->exit(0);
    });
}

Widget::~Widget()
{
    delete ui;
}

//重写父类的虚函数,父类中默认的实现 是忽略右键菜单事件,重写之后就可以处理右键菜单
void Widget::contextMenuEvent(QContextMenuEvent *event)
{
    //弹出右键菜单
    mExitMenu->exec(QCursor::pos());

    //调用accpet表示:这个事件我已经处理了,不需要向上传递了
    event->accept();
}

```

# 4.2 界面布局

![image-20260628140102668](./image-20260628140102668.png)

![image-20260628093356264](./image-20260628093356264.png)

![image-20260628093338545](./image-20260628093338545.png)

![image-20260628093417796](./image-20260628093417796.png)

![image-20260628093432080](./image-20260628093432080.png)

![image-20260628093447700](./image-20260628093447700.png)

```c++
QWidget#widget{
		border-image:	url(:res/background.png)
}

 
```

# 4.3 窗口随鼠标移动

## widget.h

```c++
#ifndef WIDGET_H
#define WIDGET_H

#include <QWidget>
#include <QContextMenuEvent>

QT_BEGIN_NAMESPACE
namespace Ui {
class Widget;
}
QT_END_NAMESPACE

class Widget : public QWidget
{
    Q_OBJECT

public:
    Widget(QWidget *parent = nullptr);
    ~Widget();
protected:
    //捕获用户在窗口上点击鼠标右键（或触控板双指点击）的事件，以便弹出自定义的菜单（右键菜单/上下文菜单）。
    void contextMenuEvent(QContextMenuEvent *event) override;
    void mousePressEvent(QMouseEvent *event) override;
    void mouseMoveEvent(QMouseEvent *event) override;

private:
    Ui::Widget *ui;
    QMenu *mExitMenu;   //右键退出的菜单
    QAction *mExitAct;  //右键退出的菜单项(退出的行为)

    //窗口移动时,鼠标与窗口左上角的偏移
    QPoint mOffset;
};
#endif // WIDGET_H

```

## widget.cpp

```c++
#include "widget.h"
#include "ui_widget.h"
#include <QMenu>
#include <QAction>

Widget::Widget(QWidget *parent)
    : QWidget(parent)
    , ui(new Ui::Widget)
{
    ui->setupUi(this);

    // 加上这一句：强制让 QWidget 遵循样式表去绘制背景
    this->setAttribute(Qt::WA_StyledBackground, true);

    setWindowFlag(Qt::FramelessWindowHint);//设置窗口无边框
    setFixedSize(width(),height());         //设置固定窗口大小
    //构建右键菜单,退出程序
    mExitMenu = new QMenu(this);
    mExitAct = new QAction();

    mExitAct->setText("退出");
    mExitAct->setIcon(QIcon(":/res/close.png"));

    mExitMenu->addAction(mExitAct);

    connect(mExitAct,&QAction::triggered,this,[=](){
        qApp->exit(0);
    });
}

Widget::~Widget()
{
    delete ui;
}

//重写父类的虚函数,父类中默认的实现 是忽略右键菜单事件,重写之后就可以处理右键菜单
void Widget::contextMenuEvent(QContextMenuEvent *event)
{
    //弹出右键菜单
    mExitMenu->exec(QCursor::pos());

    //调用accpet表示:这个事件我已经处理了,不需要向上传递了
    event->accept();
}

void Widget::mousePressEvent(QMouseEvent *event)
{
    //event->globalPos() 是鼠标在屏幕上的全局坐标。
    //this->pos() 是当前窗口左上角在屏幕上的全局坐标
    //两者相减，得到鼠标点击位置到窗口左上角的向量，保存在成员变量 mOffset 中。
    //鼠标到窗口左上角偏移量 mOffset
    mOffset = event->globalPos() - this->pos();
}

void Widget::mouseMoveEvent(QMouseEvent *event)
{
    //event->globalPos() 得到鼠标移动后的新坐标
    //此时窗口应该在:该鼠标新位置-鼠标到窗口左上角那个固定的偏移量
    this->move(event->globalPos()-mOffset);
}

```

# 4.4 http 请求天气数据

![image-20260628181840513](./image-20260628181840513.png)

![image-20260628184144806](./image-20260628184144806.png)

![image-20260628184200408](./image-20260628184200408.png)

![image-20260628184228170](./image-20260628184228170.png)

# 4.5 解析JSON并展示数据

![image-20260628190159650](./image-20260628190159650.png)

昨天0/今天1/明天2/3/4/5

week

日期date

天气type

**天气质量aqi**

风向/风力fl/fx

最高温和最低温hight/low



今天的日期date

今天的城市city

今天的温度wendu

今天的感冒指数ganmao

最高温和最低温hight/low

城市city

天气type

风向/风力

pm2.5 pm25

湿度 shidu 

**空气质量 quality**

# 4.6更新UI界面



# 4.7  获取城市编码

输入北京时，获得北京对应的城市编码,需要类weatherTool

# 4.8绘制温度曲线

![image-20260629115853855](./image-20260629115853855.png)

## (1) 安装事件过滤器

## (2) 重写eventFilter方法 

![image-20260629115915136](./image-20260629115915136.png)

![image-20260629115931690](./image-20260629115931690.png)

### 1. Widget构造函数中:安装事件过滤器

```c++
    //(1)安装事件过滤器 给标签添加事件过滤器
    // 参数指定为this,也就是当前窗口对象mainwindow
    ui->lblHighCurve->installEventFilter(this);
    ui->lblLowCurve->installEventFilter(this);
```

**事件过滤器（Event Filter）**，通俗点说就是**“事件的保安/中间人”**。

它允许一个对象（比如你这里的 `this` 主窗口）**在事件到达目标对象（`lblHighCurve` 标签）之前，先偷看一眼，甚至直接拦截处理掉**，而不需要去修改目标对象（标签）本身的代码。

想象一下公司前台：

- **目标对象（标签）**：就是公司里的某个员工（`lblHighCurve`）。
- **事件（Paint）**：就是寄给这个员工的快递（系统要求标签重绘）。
- **事件过滤器（`this` 主窗口）**：就是前台保安。

你执行 `ui->lblHighCurve->installEventFilter(this);`，相当于给这个员工配置了一个专属保安（主窗口）。
从此，所有寄给这个员工的快递，**必须先经过保安的手**。保安可以：

1. **自己拆开处理**（`return true`），快递不给员工。
2. **看一眼再转交**（`return false`），让员工自己处理。
3. **偷摸干点别的**（比如画个曲线），再转交。

### 2. Widget头文件中声明:重写父类的eventsfilter方法

```c#
 	//(2)重写父类的 eventsfilter 方法
    bool eventFilter(QObject* watched,QEvent* event) override;
```

### 3. widget源文件中实现:重写父类的eventsfilter方法

```c++
//实现eventsfilter方法重写
bool Widget::eventFilter(QObject *watched, QEvent *event)
{
    if(watched==ui->lblHighCurve && event->type()==QEvent::Paint){
        paintHighCurve(); // 保安拦下“重绘事件”，亲自画了温度曲线
    }
    if(watched==ui->lblLowCurve && event->type()==QEvent::Paint){
        paintLowCurve();	// 保安拦下“重绘事件”，亲自画了温度曲线
    }
    //return QWidget::eventFilter(watched,event);//事件继续往后传
    return true;//这样事件就被“保安”吃掉了，员工再也收不到快递。
}

```

当系统需要重绘这两个标签时：

1. 系统发出 `QEvent::Paint` 事件给 `lblHighCurve`。
2. 因为安装了过滤器，事件先飞到 `Widget::eventFilter` 里。
3. 函数发现 `watched` 是 `lblHighCurve`，且是 `Paint` 事件，于是调用 `paintHighCurve()`，**直接用 QPainter 在标签上画出了曲线**。
4. 最后 `return QWidget::eventFilter(...)`，这相当于**“保安把快递转交给员工”**（基类默认返回 `false`，表示事件继续传递）。

## (3) 绘制曲线

![image-20260629120429804](./image-20260629120429804.png)

![image-20260629120451603](./image-20260629120451603.png)

![image-20260629120512177](./image-20260629120512177.png)

### 最高温曲线绘图

```c++
//最高温曲线绘图
void Widget::paintHighCurve()
{
    QPainter painter(ui->lblHighCurve);

    //抗锯齿
    painter.setRenderHint(QPainter::Antialiasing,true);

    //1. 获取x坐标
    int pointX[6] = {0};
    for(int i = 0;i<6;i++){
        pointX[i] = mWeekList[i]->pos().x() + mWeekList[i]->width()/2;

    }
    //2. 获取y坐标
    int tempSum = 0;
    int tempAvearge = 0;
    for(int i = 0;i<6;i++){
        tempSum += mDay[i].high;
    }
    tempAvearge = tempSum/6;//最高温的平均值//平均值在label中间

    //计算y坐标
    int pointY[6] = {0};
    int yCenter = ui->lblHighCurve->height()/2;
    for(int i = 0;i<6;i++){
        pointY[i] = yCenter - ((mDay[i].high-tempAvearge)*INCREMENT);
    }

    //3. 开始绘制
    //3.1 初始化画笔相关
    painter.save();                     // ① 保存画家默认状态(此时画笔还是默认黑色，因为还没setPen

    QPen pen = painter.pen();           // ② 获取当前画笔的副本（默认黑色、1px等）
    pen.setWidth(1);                    // ③ 修改画笔副本（宽度设为1）//设置画笔的宽度
    pen.setColor(QColor(255,170,0));    // ④ 修改画笔副本（颜色设为橙色）//设置画笔的颜色

    painter.setPen(pen);                // ⑤ 设置画家的画笔
    painter.setBrush(QColor(255,170,0));// ⑥ 设置画家画刷的颜色-内部填充的颜色


    //3.2 画点、写文字
    for(int i = 0;i<6;i++){
        //显示点
        painter.drawEllipse(QPoint(pointX[i],pointY[i]),POINT_RADIUS,POINT_RADIUS);

        //显示温度文本
        painter.drawText(pointX[i]-TEXT_OFFSET_X,pointY[i]-TEXT_OFFSET_Y,
                         QString::number(mDay[i].high) + "°");
    }

    //3.3 绘制曲线
    for(int i = 0;i<5;i++){
        //昨天到今天是虚线
        if(i == 0){
            pen.setStyle(Qt::DotLine);//虚线
            painter.setPen(pen);
        }
        else{
            pen.setStyle(Qt::SolidLine);//实线
            painter.setPen(pen);
        }
        painter.drawLine(pointX[i],pointY[i],pointX[i+1],pointY[i+1]);
    }
    painter.restore();       // ⑦ 恢复save()原始状态
}
```

### 最低温曲线绘图

```c++
//最低温曲线绘图
void Widget::paintLowCurve()
{
    QPainter painter(ui->lblLowCurve);

    //抗锯齿
    painter.setRenderHint(QPainter::Antialiasing,true);

    //1. 获取x坐标
    int pointX[6] = {0};
    for(int i = 0;i<6;i++){
        pointX[i] = mWeekList[i]->pos().x() + mWeekList[i]->width()/2;

    }
    //2. 获取y坐标
    int tempSum = 0;
    int tempAvearge = 0;
    for(int i = 0;i<6;i++){
        tempSum += mDay[i].low;
    }
    tempAvearge = tempSum/6;//最高温的平均值//平均值在label中间

    //计算y坐标
    int pointY[6] = {0};
    int yCenter = ui->lblLowCurve->height()/2;
    for(int i = 0;i<6;i++){
        pointY[i] = yCenter - ((mDay[i].low-tempAvearge)*INCREMENT);
    }

    //3. 开始绘制
    //3.1 初始化画笔相关
    painter.save();                     // ① 保存画家默认状态(此时画笔还是默认黑色，因为还没setPen

    QPen pen = painter.pen();           // ② 获取当前画笔的副本（默认黑色、1px等）
    pen.setWidth(1);                    // ③ 修改画笔副本（宽度设为1）//设置画笔的宽度
    pen.setColor(QColor(0,255,255));    // ④ 修改画笔副本（颜色设为蓝色）//设置画笔的颜色

    painter.setPen(pen);                // ⑤ 设置画家的画笔
    painter.setBrush(QColor(0,255,255));// ⑥ 设置画家画刷的颜色-内部填充的颜色

    //3.2 画点、写文字
    for(int i = 0;i<6;i++){
        //显示点
        painter.drawEllipse(QPoint(pointX[i],pointY[i]),POINT_RADIUS,POINT_RADIUS);

        //显示温度文本
        painter.drawText(pointX[i]-TEXT_OFFSET_X,pointY[i]-TEXT_OFFSET_Y,
                         QString::number(mDay[i].high) + "°");
    }

    //3.3 绘制曲线
    for(int i = 0;i<5;i++){
        //昨天到今天是虚线
        if(i == 0){
            pen.setStyle(Qt::DotLine);//虚线
            painter.setPen(pen);
        }
        else{
            pen.setStyle(Qt::SolidLine);//实线
            painter.setPen(pen);
        }

        painter.drawLine(pointX[i],pointY[i],pointX[i+1],pointY[i+1]);
    }
    painter.restore();        // ⑦ 恢复save()原始状态
}
```

## (4) 说明

![image-20260629122231350](./image-20260629122231350.png)

### 解析完数据后调用update方法,完成曲线的绘制

1. **网络请求** → 拿到天气 JSON 数据。

2. **`parseJson` 解析** → 把数据填进 `mDay[0]` ~ `mDay[5]` 数组（高低温度变了）。

3. **更新 UI 文字** → 调用 `updateUI()` 更新标签上的文字、图标。

4. **请求重绘曲线** → 

   ```c++
   ui->lblHighCurve->update();
   ui->lblLowCurve->update();
   ```

5. **触发绘制函数** → `update()` 产生 `Paint` 事件，被事件过滤器拦截，调用 `paintHighCurve()` 和 `paintLowCurve()`。

### 如果不调用 `update()` 会怎样？

**曲线永远停留在上一次的数据，不会改变。**

因为 `paintHighCurve()` 函数**不是自动执行的**。它只在两种情况下会被调用：

- **情况一**：窗口第一次显示。
- **情况二**：你手动调用 `update()` 触发了重绘。

如果你在 `parseJson` 里注释掉这两行 `update()`，即使 `mDay[0].high` 从 20℃ 变成了 35℃，标签上依然画着旧的 20℃ 曲线。**数据变了，但画布没刷新。**

# 扩展:

首次运行时加载https://ipinfo.io/json的IP地址,之后按照用户输入的地址来缓存
